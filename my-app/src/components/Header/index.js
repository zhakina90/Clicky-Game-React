import React from "react";
import("./style.css");

function Header() {
    return (
        <header className="App-header">
            <h1>Clicky Game!</h1>
            <h2>Click on an image to earn points, but don't click no more than once!</h2>
        </header>
    );
}
export default Header; 